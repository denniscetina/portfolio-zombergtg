---
const { title, company, period, description, bullets } = Astro.props;
---

<div class="mb-4">
    <h3 class="h5">{title}</h3>
    <p class="text-muted mb-1">{company} | {period}</p>
    <p>{description}</p>
    <ul>
        {bullets.map(bullet => <li>{bullet}</li>)}
    </ul>
</div>